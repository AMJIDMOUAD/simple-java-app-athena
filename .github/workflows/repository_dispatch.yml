name: repository dispatch workflow

on:
  workflow_dispatch:
  repository_dispatch:
    types: [build]

jobs:
  dispatch_job:
    runs-on: ubuntu-latest

    steps:
      - name: Print message
        run: echo "repository_dispatch build event triggered"