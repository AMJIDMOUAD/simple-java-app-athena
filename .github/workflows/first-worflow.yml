name: java workflow
on:
  push:
    branches:
      "main"
jobs:
  main_workflow:
    name : main_workflow
    runs-on: java-latest
    
    steps:
      - name: chekout step
        run: actions/checkout@v3

      - name: mvn clean 
        run : mvn clean package